export interface Repos {
    id: number
    name: string
    html_url: string
    language: string
}

export interface Profile {
    login: string
    avatar_url: string
    html_url: string
    name: string
    location: string
    bio: string
}